# Test rules

Apply when a change touches behavior.

- Start with the smallest relevant verification.
- Add or update tests when behavior changes.
- If a test cannot run locally, explain the missing dependency.
- For generated code, inspect the diff before accepting green output.
- Never mark a task done only because files were edited.

